# Django General Settings
SECRET_KEY=your-production-secret-key-here
DEBUG=False
ALLOWED_HOSTS=api.corx.ae,www.api.corx.ae,corx.ae,www.corx.ae,127.0.0.1,localhost

# CORS Settings (Allow your frontend domain to make API calls to backend)
CORS_ALLOWED_ORIGINS=https://corx.ae,https://www.corx.ae,https://api.corx.ae,https://www.api.corx.ae,http://localhost:5173
CORS_ALLOW_ALL_ORIGINS=False

# Database Settings
# Choices for DB_ENGINE:
# - SQLite (default for testing): django.db.backends.sqlite3
# - MySQL / MariaDB (Hostinger Database): django.db.backends.mysql
DB_ENGINE=django.db.backends.mysql
DB_NAME=u123456789_healthcare
DB_USER=u123456789_user
DB_PASSWORD=your-hostinger-db-password
DB_HOST=127.0.0.1
DB_PORT=3306
